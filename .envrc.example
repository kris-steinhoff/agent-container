export COMPOSE_FILE=docker-compose.yml:docker-compose.local.yml
export AGENT_UID=$(id -u)

# Cloud path (`./up cloud`) config lives in terraform/terraform.tfvars now (region,
# subnet, aws_profile), rendered into .up.toml by `terraform apply` — nothing to
# set here. See README "Running on AWS Fargate".
